BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[Workflow] (
    [id] INT NOT NULL IDENTITY(1,1),
    [email] NVARCHAR(1000) NOT NULL,
    [name] NVARCHAR(1000),
    CONSTRAINT [Workflow_pkey] PRIMARY KEY CLUSTERED ([id]),
    CONSTRAINT [Workflow_email_key] UNIQUE NONCLUSTERED ([email])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
